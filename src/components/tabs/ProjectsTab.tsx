import './ProjectsTab.css'

/**
 * Placeholder for the "Projects" tab. Backed by GET /api/projects and
 * GET /api/prompts (wired later); content intentionally blank for now.
 */
export default function ProjectsTab() {
  return (
    <section className="tab-panel">
      <h2>Projects</h2>
    </section>
  )
}
